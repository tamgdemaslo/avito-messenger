# Проверка работы API

## Проблема

При клике на чат:
- JavaScript вызывает `/api/messages`
- Показывается загрузка
- Но сообщения не отображаются

## Что проверить:

### 1. Откройте DevTools (F12)

Нажмите F12 в браузере

### 2. Перейдите на вкладку Console

Посмотрите ошибки JavaScript (красным цветом)

### 3. Перейдите на вкладку Network

- Найдите запрос к `/api/messages`
- Нажмите на него
- Посмотрите вкладку "Response" - что он вернул?

### 4. Проверьте что возвращает /api/chats

В Network найдите `/api/chats` и посмотрите Response

---

## Возможные проблемы:

### 1. API возвращает ошибку

Например:
```json
{"error": "Could not get user ID"}
```

**Решение:** Проверить переменные в Railway

### 2. Формат данных не соответствует ожиданиям

JavaScript ожидает определенную структуру, а API возвращает другую.

### 3. JavaScript функция selectChat не определена

Ошибка: `selectChat is not defined`

**Решение:** Проверить что `static/js/app.js` загружен

---

## Быстрая проверка:

Откройте в другой вкладке:
```
https://avito.tamgdemaslocrm.ru/api/chats
```

Посмотрите что там - должен быть JSON с чатами.

Затем откройте:
```
https://avito.tamgdemaslocrm.ru/api/profile
```

Должна быть информация о вашем профиле.

---

Проверьте DevTools и покажите что в Response для `/api/messages`! 🔍

