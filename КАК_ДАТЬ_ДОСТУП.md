# Как дать доступ для автоматической загрузки

## Я не могу получить прямой доступ к GitHub

Но могу автоматизировать загрузку через ваш токен!

---

## ✅ Способ 1: Создайте Personal Access Token (самый простой)

### Шаг 1: Создайте токен

1. Откройте: https://github.com/settings/tokens/new
2. **Note:** `avito-messenger-upload`
3. **Expiration:** `30 days` (или `No expiration`)
4. **Select scopes:** ✅ отметьте `repo` (полный доступ)
5. Нажмите **"Generate token"**
6. **Скопируйте токен!** (ghp_xxxxx...)

### Шаг 2: Запустите автоматическую загрузку

```bash
cd /Users/ilaeliseenko/Desktop/avito-messenger
./auto_upload.sh
```

Когда попросит авторизацию:
- **Username:** `tamgdemaslo`
- **Password:** вставьте ваш токен

### Шаг 3: Сохраните токен (чтобы не вводить каждый раз)

```bash
git config --global credential.helper osxkeychain
```

После первого ввода токен сохранится автоматически!

---

## ✅ Способ 2: GitHub CLI (еще проще)

### Установите GitHub CLI:

```bash
brew install gh
```

### Авторизуйтесь:

```bash
gh auth login
```

Выберите:
- GitHub.com
- HTTPS
- Login with a web browser

### Загрузите файлы:

```bash
cd /Users/ilaeliseenko/Desktop/avito-messenger
git add .
git commit -m "Update project"
git push origin main
```

Не потребуется токен - GitHub CLI авторизует автоматически!

---

## ✅ Способ 3: Дайте мне токен (временно)

Если создадите токен и дадите мне, я смогу загрузить файлы.

**НО:** токен дает полный доступ к вашим репозиториям, поэтому:
1. Создайте токен с ограниченным сроком (30 days)
2. После загрузки удалите токен: https://github.com/settings/tokens

---

## 🎯 Рекомендую Способ 2 (GitHub CLI)

Самый безопасный и удобный вариант!

```bash
brew install gh
gh auth login
cd /Users/ilaeliseenko/Desktop/avito-messenger
git push origin main
```

Всё! 🚀

---

Какой способ выберете?

