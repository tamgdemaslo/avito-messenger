# 🚀 Быстрый старт: Деплой на Railway

## ✅ Подготовка завершена!

Все файлы готовы для деплоя. Git репозиторий инициализирован.

## Следующие шаги:

### 1. Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Название репозитория: `avito-messenger`
3. Описание: `Avito Messenger - веб-приложение для работы с сообщениями Avito`
4. Оставьте **Public** (или Private, если хотите)
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
6. Нажмите "Create repository"

### 2. Загрузите код на GitHub

GitHub покажет команды. Выполните в терминале:

```bash
cd /Users/ilaeliseenko/Desktop/avito-messenger

# Добавьте remote (замените YOUR-USERNAME на ваш username GitHub)
git remote add origin https://github.com/YOUR-USERNAME/avito-messenger.git

# Отправьте код
git branch -M main
git push -u origin main
```

### 3. Задеплойте на Railway

#### Вариант A: Через веб-интерфейс (проще)

1. Откройте https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub
4. Выберите "Deploy from GitHub repo"
5. Выберите репозиторий `avito-messenger`
6. Railway автоматически задеплоит приложение

#### Вариант B: Через CLI (быстрее)

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Инициализируйте проект
cd /Users/ilaeliseenko/Desktop/avito-messenger
railway init

# Задеплойте
railway up
```

### 4. Настройте переменные окружения в Railway

В Railway Dashboard → Variables добавьте:

```
AVITO_CLIENT_ID=HHiafcJb3rn8agxhzM8g
AVITO_CLIENT_SECRET=DusHGZF4gADWpNwIHbeefVNwxqoUXe5i_LQ2_g2o
SECRET_KEY=your-random-secret-key-here-change-this
FLASK_ENV=production
```

**Генерация SECRET_KEY:**
```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

### 5. Получите URL и добавьте REDIRECT_URI

1. После деплоя Railway выдаст URL, например:
   ```
   https://avito-messenger-production.up.railway.app
   ```

2. Добавьте в Railway переменную:
   ```
   AVITO_REDIRECT_URI=https://avito-messenger-production.up.railway.app/callback
   ```

3. Railway автоматически перезапустит приложение

### 6. Настройте Redirect URI в Avito

1. Вернитесь в форму Avito "Новое приложение"
2. В поле "Redirect URL" введите:
   ```
   https://avito-messenger-production.up.railway.app/callback
   ```
   (используйте ваш реальный URL от Railway)

3. Нажмите "Запросить доступ"

### 7. Готово! 🎉

Откройте ваше приложение:
```
https://avito-messenger-production.up.railway.app
```

Нажмите "Войти через Avito" и начните работать с сообщениями!

---

## Альтернатива: Render.com

Если не хотите использовать Railway, можно задеплоить на Render:

1. Откройте https://render.com
2. Создайте Web Service
3. Подключите GitHub репозиторий
4. Render автоматически определит Flask приложение
5. Добавьте переменные окружения
6. Получите URL типа `https://avito-messenger.onrender.com`

---

## Нужна помощь?

Если возникнут проблемы:
1. Проверьте логи в Railway/Render Dashboard
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте, что `Procfile` и `requirements.txt` на месте

Всё готово для деплоя! 🚀

