# 🚀 Деплой на Railway.app

## Шаг 1: Подготовка (уже сделано)

✅ Все необходимые файлы созданы:
- `Procfile` - для запуска приложения
- `runtime.txt` - версия Python
- `requirements.txt` - обновлен (добавлен gunicorn)
- `.env.example` - пример переменных окружения

## Шаг 2: Создайте Git репозиторий

```bash
cd /Users/ilaeliseenko/Desktop/avito-messenger

# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit - Avito Messenger"
```

## Шаг 3: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Назовите репозиторий: `avito-messenger`
4. Нажмите "Create repository"

5. Свяжите локальный репозиторий с GitHub:
```bash
git remote add origin https://github.com/ваш-username/avito-messenger.git
git branch -M main
git push -u origin main
```

## Шаг 4: Деплой на Railway

### 4.1. Создайте аккаунт

1. Перейдите на https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub

### 4.2. Создайте новый проект

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий `avito-messenger`
4. Railway автоматически определит Flask приложение

### 4.3. Настройте переменные окружения

В настройках проекта Railway добавьте:

```
AVITO_CLIENT_ID=HHiafcJb3rn8agxhzM8g
AVITO_CLIENT_SECRET=DusHGZF4gADWpNwIHbeefVNwxqoUXe5i_LQ2_g2o
SECRET_KEY=your-random-secret-key-here
FLASK_ENV=production
```

**Важно:** `AVITO_REDIRECT_URI` добавим после получения URL от Railway

### 4.4. Получите URL приложения

1. После деплоя Railway выдаст URL типа:
   ```
   https://avito-messenger-production.up.railway.app
   ```

2. Добавьте переменную окружения:
   ```
   AVITO_REDIRECT_URI=https://avito-messenger-production.up.railway.app/callback
   ```

3. Railway автоматически перезапустит приложение

## Шаг 5: Настройте Redirect URI в Avito

1. Вернитесь в форму создания приложения Avito
2. В поле "Redirect URL" введите:
   ```
   https://avito-messenger-production.up.railway.app/callback
   ```
   (используйте ваш реальный URL от Railway)

3. Нажмите "Запросить доступ"

## Шаг 6: Проверьте работу

1. Откройте ваше приложение:
   ```
   https://avito-messenger-production.up.railway.app
   ```

2. Нажмите "Войти через Avito"

3. Авторизуйтесь и начните работать с сообщениями!

## Альтернатива: Быстрый деплой без GitHub

Если не хотите создавать GitHub репозиторий, используйте Railway CLI:

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Инициализируйте проект
railway init

# Задеплойте
railway up
```

## Troubleshooting

### Проблема: Приложение не запускается

**Решение:** Проверьте логи в Railway Dashboard

### Проблема: 502 Bad Gateway

**Решение:** Убедитесь, что:
- `Procfile` содержит `web: gunicorn app:app`
- `gunicorn` добавлен в `requirements.txt`

### Проблема: Переменные окружения не работают

**Решение:** Перезапустите приложение после добавления переменных

## Стоимость

Railway предоставляет:
- **$5 бесплатных кредитов** каждый месяц
- Этого достаточно для небольшого приложения
- Если кредиты закончатся, приложение остановится до следующего месяца

## Готово!

После деплоя ваше приложение будет доступно 24/7 с HTTPS и реальным доменом.

