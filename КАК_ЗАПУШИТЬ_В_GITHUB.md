# 🚀 Как запушить изменения в GitHub (если авторизация через Google)

## Проблема
Если вы авторизованы в GitHub через Google, у вас нет обычного пароля для `git push`.

## ✅ Решение: Personal Access Token (PAT)

### Шаг 1: Создайте Personal Access Token

1. Откройте GitHub: https://github.com
2. Нажмите на ваш аватар (правый верхний угол) → **Settings**
3. В левом меню прокрутите вниз и нажмите **Developer settings**
4. Нажмите **Personal access tokens** → **Tokens (classic)**
5. Нажмите **Generate new token** → **Generate new token (classic)**
6. Заполните форму:
   - **Note:** `Avito Messenger Push` (любое название)
   - **Expiration:** Выберите срок действия (например, 90 дней или No expiration)
   - **Select scopes:** Отметьте `repo` (это даст доступ к репозиториям)
7. Нажмите **Generate token** внизу
8. ⚠️ **ВАЖНО:** Скопируйте токен сразу! Он показывается только один раз!
   - Токен будет выглядеть примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Используйте токен вместо пароля

Теперь в терминале выполните:

```bash
cd /Users/ilaeliseenko/avito-messenger
git push origin main
```

Когда Git попросит:
- **Username:** введите ваш GitHub username (например: `tamgdemaslo`)
- **Password:** вставьте Personal Access Token (НЕ пароль!)

---

## 🔐 Альтернатива: Настроить SSH (рекомендуется)

Если не хотите каждый раз вводить токен, настройте SSH:

### 1. Проверьте, есть ли SSH ключ:

```bash
ls -la ~/.ssh/id_*.pub
```

Если файл есть (например, `id_ed25519.pub`), переходите к шагу 3.

### 2. Создайте SSH ключ (если нет):

```bash
ssh-keygen -t ed25519 -C "ваш_email@example.com"
```

Нажмите Enter для всех вопросов (или введите пароль для ключа).

### 3. Скопируйте публичный ключ:

```bash
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод (начинается с `ssh-ed25519 ...`)

### 4. Добавьте ключ в GitHub:

1. GitHub → Settings → **SSH and GPG keys**
2. Нажмите **New SSH key**
3. **Title:** `MacBook Air` (любое название)
4. **Key:** вставьте скопированный ключ
5. Нажмите **Add SSH key**

### 5. Переключите репозиторий на SSH:

```bash
cd /Users/ilaeliseenko/avito-messenger
git remote set-url origin git@github.com:tamgdemaslo/avito-messenger.git
git remote -v  # Проверьте, что изменилось
```

### 6. Теперь push будет работать без пароля:

```bash
git push origin main
```

---

## 📝 Быстрый способ (через терминал Cursor)

Если у вас уже настроен SSH ключ, просто выполните в терминале Cursor:

```bash
cd /Users/ilaeliseenko/avito-messenger
git remote set-url origin git@github.com:tamgdemaslo/avito-messenger.git
git push origin main
```

---

## ✅ После успешного push

1. Проверьте GitHub: https://github.com/tamgdemaslo/avito-messenger
2. Должен появиться новый коммит
3. Railway автоматически начнет деплой (через 1-2 минуты)
