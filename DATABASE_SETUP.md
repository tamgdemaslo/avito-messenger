# 💾 Настройка постоянного хранилища данных

## ⚠️ Проблема с SQLite на Railway

### Текущая ситуация:
```
SQLite (customers.db) → временный контейнер
```

**Данные теряются при:**
- ❌ Redeploy (новый билд)
- ❌ Git push (обновление кода)
- ❌ Перезапуск сервиса

**Данные сохраняются при:**
- ✅ Обычной работе приложения
- ✅ Restart без rebuild

---

## ✅ Решение: PostgreSQL от Railway (БЕСПЛАТНО)

### Преимущества PostgreSQL:
- ✅ **Данные постоянные** (не теряются никогда)
- ✅ **Бесплатный тариф** (500 MB)
- ✅ **Автоматический backup**
- ✅ **Быстрее** чем SQLite для множества записей

---

## 🚀 Настройка за 3 шага

### Шаг 1: Добавить PostgreSQL на Railway

1. Откройте ваш проект на Railway
2. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Подождите ~30 секунд

### Шаг 2: Подключить к Flask сервису

Railway **автоматически** добавит переменную окружения:
```
DATABASE_URL=postgresql://user:password@host:port/database
```

**Ничего делать не нужно** - связь создается автоматически!

### Шаг 3: Redeploy Flask сервиса

1. Подождите автоматического redeploy (~2-3 мин)
2. Или нажмите **"Deploy"** вручную

В логах увидите:
```
📊 Using PostgreSQL database
✅ Database initialized
```

---

## 🔍 Как проверить что работает PostgreSQL

### В логах Flask сервиса:
```
📊 Using PostgreSQL database  ← Правильно!
```

Или:
```
📊 Using SQLite database  ← Еще не подключен
```

### Проверка данных:
1. Откройте чат
2. Введите VIN, телефон, комментарий
3. Сделайте git push (redeploy)
4. Откройте тот же чат
5. **Данные сохранились!** ✅

---

## 💡 Как работает автоопределение

```python
DATABASE_URL = os.environ.get('DATABASE_URL')

if DATABASE_URL:
    # PostgreSQL (Railway)
    USE_POSTGRES = True
else:
    # SQLite (локально)
    USE_POSTGRES = False
```

**На Railway:** автоматически PostgreSQL  
**Локально:** автоматически SQLite

---

## 📊 Сравнение

| Параметр | SQLite | PostgreSQL |
|----------|--------|------------|
| **Постоянство** | ❌ Временное | ✅ Постоянное |
| **После redeploy** | ❌ Теряется | ✅ Сохраняется |
| **Цена** | Бесплатно | Бесплатно (500 MB) |
| **Настройка** | Авто | 1 клик |
| **Backup** | Нет | ✅ Автоматический |

---

## 🎯 Что будет храниться

Для каждого клиента:
- ✅ Имя
- ✅ VIN номер
- ✅ Телефон
- ✅ Комментарии
- ✅ Источник (telegram/whatsapp/avito)
- ✅ Дата создания/обновления

---

## ⚡ Быстрая настройка

1. **Railway** → ваш проект → **"+ New"** → **"Database"** → **"PostgreSQL"**
2. Подождите 30 секунд
3. Готово! Данные теперь постоянные

---

**После добавления PostgreSQL данные будут сохраняться навсегда!** 💾✅

