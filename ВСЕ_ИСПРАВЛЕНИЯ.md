# ✅ Все исправления OAuth URL

## Что было исправлено:

### 1. Authorization URL
**Было:** `https://api.avito.ru/oauth/authorize`
**Стало:** `https://www.avito.ru/oauth`

### 2. Убран /authorize
**Было:** `{AVITO_AUTH_URL}/authorize?...`
**Стало:** `{AVITO_AUTH_URL}?...`

### 3. Token URL
**Было:** `{AVITO_AUTH_URL}/token` (неправильно)
**Стало:** `{AVITO_API_URL}/token` (правильно)

---

## Итоговые URL согласно документации:

- **Authorization URL:** `https://www.avito.ru/oauth` (или `https://avito.ru/oauth`)
- **Token URL:** `https://api.avito.ru/token`

---

## Что обновить на GitHub:

Откройте: https://github.com/tamgdemaslo/avito-messenger/edit/main/app.py

### Изменение 1 (строка 40):
```python
AVITO_AUTH_URL = "https://www.avito.ru/oauth"
```

### Изменение 2 (строки 115 и 151):
```python
auth_url = f"{AVITO_AUTH_URL}?{urlencode(params)}"
```
(без `/authorize`)

### Изменение 3 (строки 183 и 232):
```python
response = requests.post(
    f"{AVITO_API_URL}/token",  # Используем AVITO_API_URL вместо AVITO_AUTH_URL
    data=token_data,
    headers={"Content-Type": "application/x-www-form-urlencoded"}
)
```

---

## Или скопируйте весь блок:

```python
AVITO_AUTH_URL = "https://www.avito.ru/oauth"
AVITO_API_URL = "https://api.avito.ru"
```

И в функциях:
```python
auth_url = f"{AVITO_AUTH_URL}?{urlencode(params)}"
```

```python
response = requests.post(
    f"{AVITO_API_URL}/token",
    ...
)
```

---

Обновите эти 3 изменения на GitHub и всё заработает! 🚀

