# Проверка логов Railway

## Ошибка 500 - Internal Server Error

Приложение запущено, но выдает ошибку. Нужно проверить логи.

---

## Как проверить логи в Railway:

### 1. Откройте Railway Dashboard

https://railway.app/dashboard → ваш проект

### 2. Перейдите в Deployments

Нажмите на вкладку **Deployments**

### 3. Откройте последний деплой

Нажмите на последний деплой (самый верхний)

### 4. Посмотрите логи

В логах будет указана причина ошибки 500.

---

## Возможные проблемы:

### 1. Шаблоны не найдены

Ошибка: `Ошибка: шаблон не найден`

**Причина:** Файлы `index.html`, `messages.html` загружены в корень, а не в папку `templates/`

**Решение:** Нужно создать правильную структуру папок на GitHub

### 2. Статические файлы не найдены

**Причина:** CSS и JS файлы в корне, а не в `static/css/` и `static/js/`

**Решение:** То же - правильная структура папок

### 3. Переменные окружения не настроены

**Решение:** Проверьте Variables в Railway

---

## Что показать из логов:

Скопируйте из логов строки с ошибками (обычно красным цветом).

Это поможет точно определить проблему.

---

## Быстрое решение:

Если в логах видно ошибки с шаблонами, нужно исправить структуру на GitHub:

**На GitHub должно быть:**
```
templates/
  ├── index.html
  ├── messages.html
  └── test_oauth.html
static/
  ├── css/
  │   └── style.css
  └── js/
      └── app.js
```

**Сейчас (неправильно):**
```
index.html (в корне)
messages.html (в корне)
style.css (в корне)
app.js (в корне)
```

---

Проверьте логи в Railway и покажите ошибку! 🔍

