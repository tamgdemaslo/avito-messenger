# Настройка Git авторизации для GitHub

## Почему не работает авторизация через консоль?

GitHub с августа 2021 года **отключил** авторизацию по паролю для Git операций.

Ошибка которую вы видели:
```
remote: Invalid username or token. Password authentication is not supported for Git operations.
```

## ✅ Решение: Personal Access Token (PAT)

Нужно использовать **токен** вместо пароля.

---

## Способ 1: Создать и использовать Personal Access Token

### Шаг 1: Создайте токен

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните форму:
   - **Note:** `avito-messenger-deploy`
   - **Expiration:** `90 days` (или `No expiration`)
   - **Select scopes:** отметьте ✅ **`repo`** (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. **⚠️ ВАЖНО:** Скопируйте токен! Он больше не будет показан!

Токен будет выглядеть примерно так:
```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### Шаг 2: Используйте токен для push

```bash
cd /Users/ilaeliseenko/Desktop/avito-messenger

# Отправьте код
git push -u origin main
```

Когда Git попросит авторизацию:
- **Username:** `tamgdemaslo`
- **Password:** вставьте ваш токен (не пароль!)

### Шаг 3: Сохраните токен (опционально)

Чтобы не вводить токен каждый раз:

```bash
# Сохранить токен в macOS Keychain
git config --global credential.helper osxkeychain
```

После этого токен сохранится автоматически при первом использовании.

---

## Способ 2: SSH ключ (более безопасно)

### Шаг 1: Создайте SSH ключ

```bash
# Создайте SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"

# Нажмите Enter для сохранения в стандартное место
# Можно не вводить passphrase (просто Enter)
```

### Шаг 2: Добавьте SSH ключ в GitHub

```bash
# Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub
```

1. Откройте: https://github.com/settings/keys
2. Нажмите **"New SSH key"**
3. Title: `MacBook Air`
4. Key: вставьте содержимое из команды выше
5. Нажмите **"Add SSH key"**

### Шаг 3: Измените remote URL на SSH

```bash
cd /Users/ilaeliseenko/Desktop/avito-messenger

# Измените URL на SSH
git remote set-url origin git@github.com:tamgdemaslo/avito-messenger.git

# Теперь push без пароля
git push -u origin main
```

---

## Способ 3: GitHub CLI (самый простой)

### Установка

```bash
# Установите GitHub CLI
brew install gh
```

### Авторизация и push

```bash
# Авторизуйтесь
gh auth login

# Выберите:
# - GitHub.com
# - HTTPS
# - Login with a web browser

# Отправьте код
cd /Users/ilaeliseenko/Desktop/avito-messenger
gh repo sync
```

---

## Рекомендация

Для быстрого решения используйте **Способ 1 (Personal Access Token)**:

1. Создайте токен: https://github.com/settings/tokens
2. Выполните:
   ```bash
   cd /Users/ilaeliseenko/Desktop/avito-messenger
   git push -u origin main
   ```
3. Введите username и токен

Это займёт 2 минуты!

---

## После успешного push

Проверьте: https://github.com/tamgdemaslo/avito-messenger

Файлы должны появиться в репозитории.

Затем переходите к деплою на Railway! 🚀

